function BreakGunIgnore()
        SearchforGun()
        task.wait()
        if GunPath ~= nil then
            GunPath.KnifeServer.ShootGun:InvokeServer(1, 0, "AH")
        end
    end
    wait()
    pcall(BreakGunIgnore)
    end
end)
